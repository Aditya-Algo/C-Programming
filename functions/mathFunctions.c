#include <stdio.h>
#include <math.h>

int main() {

    // Square Root
    printf("%f\n", sqrt(16));

    // Round a Number
    printf("%f\n", ceil(1.3));
    printf("%f\n", floor(1.3));

    // Power
    printf("%f\n", pow(5, 2));

    return 0;
}